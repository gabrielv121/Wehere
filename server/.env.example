# Database (SQLite file)
DATABASE_URL="file:./dev.db"

# JWT secret — use a long random string in production
JWT_SECRET="change-me-in-production"

# Port
PORT=3001

# Frontend URL for password reset links, OAuth redirects, and Stripe success/cancel
# FRONTEND_URL=http://localhost:5173

# Backend URL for OAuth redirect_uri (must match URL your app is served from)
# BACKEND_URL=http://localhost:3001

# Optional: Sign in with Google — create OAuth client at https://console.cloud.google.com/apis/credentials
# Set redirect URI to: {BACKEND_URL}/api/auth/google/callback
# GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=xxx

# Optional: Sign in with Apple — configure in Apple Developer (Sign in with Apple)
# APPLE_CLIENT_ID=your.service.id (Service ID)
# APPLE_TEAM_ID=xxx
# APPLE_KEY_ID=xxx
# APPLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"

# Optional: Stripe for real payments (checkout redirects to Stripe; order created on webhook)
# Get keys at https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY=sk_test_xxxx
# For local dev, forward webhooks: stripe listen --forward-to localhost:3001/api/orders/webhook
# Then set the signing secret Stripe gives you:
# STRIPE_WEBHOOK_SECRET=whsec_xxxx

# Optional: Resend.com for sending emails (order confirmation, seller sale notification)
# Get API key at https://resend.com — then set:
# RESEND_API_KEY=re_xxxx
# RESEND_FROM="WeHere <onboarding@resend.dev>"

# Link shown to sellers for transferring tickets (e.g. Ticketmaster transfer page)
# TICKETMASTER_TRANSFER_URL=https://www.ticketmaster.com/transfer

# Ticketmaster Discovery API for event search when selling (get key at developer.ticketmaster.com)
# TICKETMASTER_API_KEY=your_apikey

# Optional: point the frontend to the WeHere API (see server/ and README)
# When set, the app uses the backend for auth, events, listings, and orders.
# VITE_API_URL=http://localhost:3001

# Optional: Seatics Maps API for interactive seat maps (see Seatics/TicketNetwork licensing)
# Get websiteConfigId and consumerKey from Seatics. Use sandbox first, then production after approval.
# VITE_SEATICS_WEBSITE_CONFIG_ID=690
# VITE_SEATICS_CONSUMER_KEY=your_sandbox_or_production_key
# VITE_SEATICS_SANDBOX=true
